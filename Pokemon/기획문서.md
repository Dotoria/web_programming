# Poketest 기획 문서

> 작성일: 2026-07-11
> 대상: `web_programming/Pokemon` 프로젝트 (JSP/Tomcat 기반 "포켓몬 유형 테스트" 웹앱)

---

## 1. 프로젝트 개요

**Poketest**는 MBTI류 성격 테스트처럼, 사용자가 여러 문항(5단계 리커트 척도: 매우 그렇다 ~ 전혀 그렇지 않다)에 답하면 18개 포켓몬 타입 점수를 합산해 **"당신의 포켓몬 타입 조합"**을 결과로 보여주는 재미용 웹앱입니다.

- 기술 스택: JSP + Servlet(Tomcat 10.1), 순수 JS/CSS, `question.csv` 기반 문항 데이터
- 개발 목적: 웹프로그래밍 학습용 개인 프로젝트 (`poketest.html`에 "웹프로그래밍 초보가 제작"이라 명시)
- 현재 상태: **초기 프로토타입.** 페이지 뼈대와 일부 스크립트만 존재하고, 핵심 기능(채점·결과·리뷰 저장)은 미구현

---

## 2. 현재 상태 분석

### 2.1 파일별 현황

| 파일 | 상태 | 비고 |
|---|---|---|
| `poketest.html` | ✅ 거의 완성 | 랜딩 페이지. 소개, 주의사항, 리뷰 폼. `시작하기` → `test.html` |
| `test.html` | 🟡 뼈대만 존재 | `question.csv`를 동기 XHR로 읽어 **문항 전체를 한 페이지 테이블로** 렌더링. "다음으로" 버튼이 `test.html`로 자기 자신을 새로고침 — 다음 문항 이동/제출 로직 없음. 상단 "processor bar 삽입 예정" 주석 → 원래는 문항별 진행형 UI를 의도했던 것으로 보임 (현재 구현과 설계 의도 불일치) |
| `question.csv` | 🔴 거의 비어있음 | 52줄 중 **2줄만 채워짐**. 값 인코딩도 행마다 다름 (`fire0~fire4` vs `fly1~fly5`) — 시작 인덱스 불일치, 타입명도 `fly` vs `type.js`의 `flying`으로 불일치 |
| `type.js` | 🔴 껍데기만 존재 | 18개 타입 카운터 변수 선언만 있고, 이 변수들을 채점에 사용하는 로직이 전혀 없음 |
| `review.jsp` | 🔴 미착수 | "jsp 작업 페이지"라는 플레이스홀더 텍스트만 있음. `poketest.html`의 리뷰 폼은 `action`이 주석 처리되어 있어 **어디에도 제출되지 않음** |
| `review.js` | 🟡 버그 있음 | `document.getElementsByName("name")`은 NodeList를 반환하므로 `.value` 접근 시 `undefined` 에러. 정규식 `/^\.{1, 20}$/`도 의도(20자 이하 검증)와 다르게 동작(이스케이프된 `.` + 공백 포함 잘못된 quantifier) |
| `poketest.jsp`, `test`(index.jsp) | 🔴 미사용/잔재 | `index.jsp`는 Tomcat 설치 기본 환영 페이지 그대로. `poketest.jsp`는 사실상 빈 파일 |
| `WEB-INF/web.xml` | 🔴 미설정 | Tomcat 기본 `web.xml` 그대로. `welcome-file-list`가 `poketest.html`을 가리키지 않고, 서블릿 매핑도 전혀 없음 |
| `tomcat.css/svg/txt`, `bg-*.png`, `favicon.ico`, `RELEASE-NOTES.txt`, `asf-logo-wide.svg` | ⚪ 잔재 | Tomcat 설치 시 기본 제공되는 샘플 웹앱 파일들. 실제 서비스에는 불필요 |
| `testInfo.txt` | ⚪ 참고자료 | MBTI/에니어그램/Big5 등 성격유형 프레임워크 정리 메모. 코드는 아니고 문항·결과 설계용 리서치 노트로 추정 |

### 2.2 핵심 문제 요약

1. **채점 엔진 부재**: 답변을 타입 점수로 환산하는 로직이 어디에도 없음 (`type.js` 변수 선언뿐).
2. **결과 페이지 부재**: 테스트를 끝까지 풀어도 "당신의 타입"을 보여줄 화면 자체가 없음 — 이 앱의 존재 이유인 핵심 기능이 빠져있음.
3. **문항 데이터 부족**: 52문항 슬롯 중 2개만 작성, 그마저 인코딩 규칙이 통일 안 됨.
4. **테스트 진행 흐름 미완성**: 한 페이지에 전체 문항을 몰아서 렌더링(설계 의도와 다름), 페이지 이동/제출 버튼이 실제로 동작하지 않음.
5. **리뷰 기능 백엔드 없음**: 폼은 있지만 서버로 전송되지 않고, `review.jsp`도 로직이 없음. 저장소(DB/파일)도 미정.
6. **배포 설정 안 됨**: `web.xml`이 기본값이라 `poketest.html`이 진입점으로 동작 안 함 (현재는 Tomcat 기본 페이지가 뜸).
7. **Tomcat 샘플 잔재**: 실제 서비스 시 정리 필요.
8. **버전 관리 없음**: 현재 git 저장소가 아님 — 이후 기능이 늘어날수록 위험.

---

## 3. 목표 아키텍처 제안

지금 단계에서 스택을 갈아엎기보다, **현재 JSP/Servlet + Tomcat 구조를 유지**하며 빈 부분을 채우는 것을 권장합니다 (학습 목적 프로젝트이고, 이미 Servlet 학습 흐름에 맞춰져 있음).

- **문항/채점**: `question.csv` → 서버(Servlet) 또는 클라이언트(JS)에서 파싱 → 사용자가 선택한 값들을 타입별로 합산
- **결과 산출**: 점수가 가장 높은 상위 1~2개 타입을 조합해 보여주는 방식 (포켓몬처럼 "불꽃/비행" 듀얼 타입 콘셉트)
- **리뷰 저장**: 학습 목적이면 CSV/텍스트 파일 저장도 충분하지만, Servlet + DB(H2 또는 MySQL) 조합으로 가면 실습 범위가 자연스럽게 확장됨
- **세션 관리**: 문항별 페이지 이동 시 사용자의 응답을 유지하려면 `HttpSession` 또는 클라이언트 sessionStorage 필요

---

## 4. 기능 로드맵 (단계별)

### Phase 1 — 데이터 설계 확정 (선행 필수) — ✅ 완료
- [x] 18개 타입 각각에 대응하는 문항 개수 결정: 단일 타입 문항 3개 × 18 = 54개 + 복합 타입(대표 포켓몬의 실제 복합타입 기반) 23개 = **총 77문항**
- [x] `question.csv` 인코딩 규칙 통일: `질문,타입[|타입2],5,4,3,2,1` 형식으로 고정. 복합 문항은 `타입1|타입2`로 표기하고 두 타입에 동시에 점수가 들어감 (자세한 근거는 `문항설계.md` 참고)
- [x] `type.js`의 타입명과 CSV의 타입명 통일 (영문 소문자 타입명으로 일관)
- [x] 문항 전량 작성 — 대표 포켓몬(한국 도감 명칭 검증 완료)의 표면/내면 성향을 기반으로 작성, `문항설계.md`에 전 과정 기록
- [x] 시간 예산: 문항당 평균 8~13초 기준 총 77문항 ≈ 12분 내외로, 20분 이내 목표에 여유 있게 도달
- [x] 타입 노출 방지: 77문항을 타입별로 묶지 않고 무작위에 가깝게 섞어 배치, 복합 문항을 섞어 타입 경계를 흐림
- [ ] (참고) Phase 3 채점 로직 구현 시, 복합 문항이 기여하는 문항 수가 타입마다 다르다는 점을 감안해 정규화 방식 결정 필요

### Phase 2 — 테스트 진행 흐름 완성 — ✅ 완료
- [x] 방식 확정: 여러 문항씩 페이지네이션 (10문항 × 8페이지, 마지막 페이지 7문항). `test.html`을 SPA 방식으로 재작성해 페이지 이동 시 전체 리로드 없이 `#question` 영역만 갱신
- [x] 사용자가 선택한 응답을 `localStorage`(`poketestAnswers`, `poketestPage`)에 누적 저장 — 새로고침해도 진행 상황 복원됨 (헤드리스 브라우저로 검증 완료)
- [x] "다음으로"/"뒤로 가기" 버튼이 실제 페이지 전환 및 라디오 체크 상태 복원까지 정상 동작 (검증 완료)
- [x] 현재 페이지의 모든 문항에 응답하지 않으면 "다음으로"가 막히고 안내 알림 표시 (검증 완료). 마지막 페이지에서는 버튼 라벨이 "제출하기"로 바뀜
- [x] 마지막 페이지 "제출하기" 클릭 시 `result.html`로 이동하도록 연결 완료

### Phase 3 — 채점 로직 & 결과 페이지 (핵심 기능) — ✅ 완료
- [x] 응답값 → 타입별 점수 합산 로직 구현. `type.js`를 18개 타입의 메타데이터(대표 포켓몬·표면/내면 성향·궁합)와 `computeTypeScores()`/`getTopTypes()` 채점 함수를 갖춘 모듈로 재작성. 복합 문항이 두 타입에 동시에 기여하는 문제는 "타입별 총점 ÷ 그 타입에 기여한 문항 수" 평균으로 정규화해서 해결
- [x] 최고 점수 타입 2개(평균 기준 상위 2개)를 조합해 "타입1 / 타입2" 형태로 결과 산출. 동점 처리 시 `TYPE_ORDER` 순서를 기준으로 결정적으로 정렬
- [x] `result.html` 신규 제작: 1·2위 타입의 대표 포켓몬·표면/내면 성향·궁합(나를 흔드는 상대/안 통하는 상대/내가 주도권 쥐는 상대), 18개 타입 전체 점수를 막대그래프로 표시. "다시 하기"(localStorage 초기화 후 재응시), "처음으로" 버튼 포함
- [x] 헤드리스 브라우저로 전체 흐름 검증: 77문항 실제 클릭 응답 → 결과 페이지 정상 렌더링 → 재응시 시 상태 초기화까지 확인. 이 과정에서 `var top = ...`가 브라우저 예약 전역 프로퍼티(`window.top`)와 충돌해 렌더링이 조용히 깨지는 버그를 발견해 `topTypes`로 수정
- [ ] 결과 공유 기능(선택) — URL 파라미터나 이미지 캡처 등. 필요 시 추가 논의

### Phase 4 — 리뷰 시스템 백엔드 — ✅ 완료
- [x] 리뷰 저장 방식 결정: **파일 기반** (`WEB-INF/reviews.dat`, 웹에서 직접 접근 불가). 유료 인프라나 별도 DB 설치 없이 동작
- [x] `review.jsp` 작성: GET 시 폼 + 리뷰 목록(최신순) 렌더링, POST 시 검증 → 저장 → `review.jsp`로 리다이렉트(새로고침 시 중복 등록 방지). 리뷰 랜딩용 UI는 `poketest.html`에서 분리해 `review.jsp`로 이동하고 링크로 연결
- [x] `review.js` 버그 수정: `getElementsByName(...)[0].value`로 수정, `nameExp`를 `/^.{0,20}$/`로 수정. `<form onsubmit="return validateReview()">` 방식으로 변경해 실제로 제출을 막을 수 있도록 함
- [x] XSS 방지: 닉네임·리뷰 내용을 출력하기 전 `escapeHtml()`로 이스케이프
- [x] 비밀번호는 SHA-256 해시로만 저장 (평문 저장 없음)
- [x] 리뷰 텍스트에 쉼표·줄바꿈·역슬래시·따옴표가 섞여도 안전하도록 필드 구분자(U+0001 제어문자)와 개행 이스케이프 방식 적용
- [x] 검증: Tomcat이 없는 환경이라 JSP 자체 실행은 못 했지만, 해싱·이스케이프·파일 읽기/쓰기 핵심 로직을 동일하게 추출한 Java 프로그램으로 28개 케이스 단위 테스트, `review.js` 클라이언트 검증은 헤드리스 브라우저로 9개 케이스 모두 확인. **JSP 페이지 자체의 최종 동작은 사용자 Tomcat 환경에서 한 번 확인 필요**

### Phase 5 — UI/UX 정리 — ✅ 완료
- [x] Tomcat 기본 잔재 파일 제거: `index.jsp`, `tomcat.css/svg/txt`, `bg-*.png`, `asf-logo-wide.svg`, `RELEASE-NOTES.txt`. 사용되지 않던 빈 스텁 `poketest.jsp`도 함께 정리
- [x] `WEB-INF/web.xml`에 `welcome-file-list`로 `poketest.html` 지정, `display-name`/`description`도 프로젝트에 맞게 수정
- [x] 모바일 반응형 스타일 보강: `poketest.css`에 미디어 쿼리 추가(버튼 크기·폰트·여백 축소), 문항 표를 `.table-scroll` 컨테이너로 감싸 좁은 화면에서도 잘림/페이지 가로 스크롤 없이 표시. 360px 폭 기준 실제 기기 에뮬레이션으로 검증 완료
- [x] 네비게이션 일관성 점검:
  - `button`/`input[type=button]`/`input[type=submit]` 스타일을 통일 (이전엔 `<button>` 태그만 스타일이 적용되고 `test.html`/`result.html`/`review.jsp`의 `<input>` 버튼은 브라우저 기본 스타일이었음)
  - `test.html` 1페이지의 "뒤로 가기"가 아무 동작 안 하던 것을 "그만두기"로 바꾸고 `poketest.html`로 이동하도록 수정
  - `result.html` 푸터의 "리뷰 남기러 가기"가 Phase 4에서 사라진 `poketest.html#review` 앵커를 가리키던 죽은 링크였던 것을 `review.jsp`로 수정
  - 모든 페이지에 `favicon.ico` 연결, `<html lang="en">`으로 남아있던 곳을 `lang="ko"`로 통일, `test.html`에도 다른 페이지와 같은 `<header>` 추가하고 타이틀을 "test"에서 "Poketest - 테스트"로 수정
- [x] 헤드리스 브라우저로 데스크톱/모바일 렌더링 및 CSS 적용 확인 완료

### Phase 6 — 마무리
- [ ] 로컬 git 저장소 초기화 및 커밋 이력 관리 시작
- [ ] 배포 방식 확정 (로컬 Tomcat vs 클라우드 호스팅)
- [ ] (참고) `test.html`은 Phase 2에서 핵심 페이지가 되었으므로 정리 대상 아님 — 남은 미사용 파일 없는지 최종 점검만 필요

---

## 5. 우선순위 제안 (MVP 기준)

가장 먼저 "테스트를 끝까지 풀면 결과가 나온다"는 최소 동작 흐름부터 완성하는 것을 권장합니다.

1. **Phase 1** (문항 10~20개만 우선 작성, 인코딩 규칙 통일)
2. **Phase 2 + 3** (진행 흐름 + 채점 + 결과 페이지) → 이 시점에 "완주 가능한 MVP" 완성
3. **Phase 5 일부** (`web.xml` 진입점 설정, 잔재 파일 정리) → 실제로 접속했을 때 제대로 보이도록
4. **Phase 4** (리뷰 백엔드) — 부가 기능이므로 MVP 이후로 미뤄도 무방
5. **Phase 6** (정리/배포)

---

## 6. 결정이 필요한 오픈 이슈

- 문항 수 및 타입당 배분(균등 배분 vs 특정 타입 가중치)
- 결과에서 단일 타입만 보여줄지, 포켓몬처럼 듀얼 타입 조합을 보여줄지
- 리뷰 저장을 파일로 할지 DB로 할지 (DB면 실습 범위 확장, 파일이면 빠르게 완성 가능)
- 진행 UI를 "문항 하나씩 + progress bar"로 갈지, 지금처럼 "여러 문항 한 페이지"로 유지할지
